/**
 * Web r3f entry: DOM Canvas on plain WebGL (no expo-gl, no Hermes
 * constraints). See r3f.ts for the import rule.
 */

export { Canvas, useFrame, useThree } from "@react-three/fiber";
export type { ThreeEvent, RootState } from "@react-three/fiber";
